import { Button, ButtonGroup } from "@chakra-ui/react";

function App() {
  return (
    <div>
      <Button colorScheme="teal" variant="outline">
        Button
      </Button>
    </div>
  );
}

export default App;
